#include <stdio.h>

#define forn(i, n) for (int i = 0; i < (n); ++i)
#define ll long long

void solve_one() {
  ;
}

int main(void) {
  int T = 1;
  scanf("%d", &T);
  while (T-- > 0) {
    solve_one();
  }
  return 0;
}
